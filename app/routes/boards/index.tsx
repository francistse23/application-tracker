export default function BoardsIndexRoute() {
  return <div>This should render the job board page</div>;
}

export function ErrorBoundary() {
  return <div>An error occurred when trying to render the job board</div>;
}
